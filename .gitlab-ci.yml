pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html css articles public/
  artifacts:
    paths:
      - public
  pages: true
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
